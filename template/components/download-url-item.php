<div class="nunas-download-item">
    <div>
        <a href="{url}" data-index="{index}" data-post-id="{post_id}" data-need-verify="{need_verify}" data-keyword="{keyword}">
            <div class="nunas-download-item-header">
                <div class="header-left">
                    {icon}
                    <div>{netdisk_name}</div>
                </div>
                {open_link}
            </div>
        </a>
        <div class="nunas-download-item-body">
            <div hide="{hide_netdisk_name}" class="resource-name">{resource_name}</div>
            <div class="netdisk-key">密码：{key} <span hide="{hide_key}" class="copy-key" data-clipboard-text="{key}"
                                                      title="复制提取码"
                                                      style="background-image: url('{copy_icon}')"></span></div>
        </div>
    </div>
</div>
